#include <stdio.h>

/**
* main - Entry point
*
* Return: returns 0 when everything works fine
*
*/

int main(void)
{
	int n;

	for (n = 150; n < 160; ++n)
	{
		putchar(n);
	}
	putchar(10);
	return (0);
}
